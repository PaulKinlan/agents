name: threat-model
class: observer
plane: [local]
containment: t0-readonly
triggers: [manual]
blocking: false
summary: Bootstrap and maintain THREAT_MODEL.md from code, git history, and issue tracking.
capabilities:
  write: false
  network: false
  browser: false
  requires: []
budget:
  max_minutes: 5
output:
  schema: report.schema.json
